/**
 * Enterprise plugins index
 * 
 * This file is a stub for OSS builds.
 * Replace this with your actual enterprise plugins when building for enterprise.
 * 
 * Example:
 * 
 * import { ssoPlugin } from './sso-plugin.js';
 * import { auditPlugin } from './audit-plugin.js';
 * 
 * export const enterprisePlugins = [
 *   ssoPlugin,
 *   auditPlugin
 * ];
 */

import type { UIGenPlugin } from '../../types/plugins.js';

/**
 * Array of enterprise plugins
 * Empty by default for OSS builds
 */
export const enterprisePlugins: UIGenPlugin[] = [];
