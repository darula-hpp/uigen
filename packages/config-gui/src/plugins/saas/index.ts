/**
 * SaaS plugins index
 * 
 * This file is a stub for OSS builds.
 * Replace this with your actual SaaS plugins when building for SaaS.
 * 
 * Example:
 * 
 * import { authPlugin } from './auth-plugin.js';
 * import { teamPlugin } from './team-plugin.js';
 * 
 * export const saasPlugins = [
 *   authPlugin,
 *   teamPlugin
 * ];
 */

import type { UIGenPlugin } from '../../types/plugins.js';

/**
 * Array of SaaS plugins
 * Empty by default for OSS builds
 */
export const saasPlugins: UIGenPlugin[] = [];
