# Panel-only Render Blueprint. For board + panel (WebSocket), use ../render.yaml instead.
# Docs: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: uigen-devboard-panel
    runtime: docker
    plan: free
    rootDir: examples/apps/nextjs/devboard-simulator/UI
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: BOARD_URL
        sync: false
